{
  "name": "@kiroton/backend",
  "version": "0.0.1",
  "description": "포트폴리오 생성기 백엔드: 수집·파이프라인·직무맞춤·API. 외부 의존은 포트로 추상화.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/node": "22.7.5"
  }
}
