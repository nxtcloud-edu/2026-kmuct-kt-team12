{
  "name": "@kiroton/shared",
  "version": "0.0.1",
  "description": "포트폴리오 생성기 팀 전체 타입 계약과 검증 코드",
  "type": "module",
  "main": "types.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/node": "22.7.5"
  }
}
